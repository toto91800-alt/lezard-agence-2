// components/resultats/charts/data/chart-data-2.ts

export type Point2 = {
    date: string
    new_followers: number
    new_likes: number
  }
  
  // ---------------------------
  // Données pour resultats-1
  // ---------------------------
  export const chartData2Resultats1 = {
    data7d: Array.from({ length: 7 }, (_, i) => ({
      date: `2024-09-0${i + 1}`,
      new_followers: Math.floor(Math.random() * (35 - 16 + 1)) + 16,
      new_likes: Math.floor(Math.random() * (20 - 10 + 1)) + 10,
    })),
    data30d: Array.from({ length: 30 }, (_, i) => ({
      date: `2024-08-${(i + 1).toString().padStart(2, "0")}`,
      new_followers: Math.floor(Math.random() * (35 - 16 + 1)) + 16,
      new_likes: Math.floor(Math.random() * (20 - 10 + 1)) + 10,
    })),
    data90d: Array.from({ length: 90 }, (_, i) => ({
      date: `2024-06-${(i + 1).toString().padStart(2, "0")}`,
      new_followers: Math.floor(Math.random() * (35 - 16 + 1)) + 16,
      new_likes: Math.floor(Math.random() * (20 - 10 + 1)) + 10,
    })),
  }
  
  // ---------------------------
  // Données pour resultats-2
  // ---------------------------
  export const chartData2Resultats2 = {
    data7d: Array.from({ length: 7 }, (_, i) => ({
      date: `2024-09-0${i + 1}`,
      new_followers: Math.floor(Math.random() * (35 - 16 + 1)) + 16,
      new_likes: Math.floor(Math.random() * (20 - 10 + 1)) + 10,
    })),
    data30d: Array.from({ length: 30 }, (_, i) => ({
      date: `2024-08-${(i + 1).toString().padStart(2, "0")}`,
      new_followers: Math.floor(Math.random() * (35 - 16 + 1)) + 16,
      new_likes: Math.floor(Math.random() * (20 - 10 + 1)) + 10,
    })),
    data90d: Array.from({ length: 90 }, (_, i) => ({
      date: `2024-06-${(i + 1).toString().padStart(2, "0")}`,
      new_followers: Math.floor(Math.random() * (35 - 16 + 1)) + 16,
      new_likes: Math.floor(Math.random() * (20 - 10 + 1)) + 10,
    })),
  }
  
  // ---------------------------
  // Données pour resultats-3
  // ---------------------------
  export const chartData2Resultats3 = {
    data7d: Array.from({ length: 7 }, (_, i) => ({
      date: `2024-09-0${i + 1}`,
      new_followers: Math.floor(Math.random() * (35 - 16 + 1)) + 16,
      new_likes: Math.floor(Math.random() * (20 - 10 + 1)) + 10,
    })),
    data30d: Array.from({ length: 30 }, (_, i) => ({
      date: `2024-08-${(i + 1).toString().padStart(2, "0")}`,
      new_followers: Math.floor(Math.random() * (35 - 16 + 1)) + 16,
      new_likes: Math.floor(Math.random() * (20 - 10 + 1)) + 10,
    })),
    data90d: Array.from({ length: 90 }, (_, i) => ({
      date: `2024-06-${(i + 1).toString().padStart(2, "0")}`,
      new_followers: Math.floor(Math.random() * (35 - 16 + 1)) + 16,
      new_likes: Math.floor(Math.random() * (20 - 10 + 1)) + 10,
    })),
  }
  
  // ---------------------------
  // Données pour resultats-4
  // ---------------------------
  export const chartData2Resultats4 = {
    data7d: Array.from({ length: 7 }, (_, i) => ({
      date: `2024-09-0${i + 1}`,
      new_followers: Math.floor(Math.random() * (12 - 5 + 1)) + 5,
      new_likes: Math.floor(Math.random() * (30 - 10 + 1)) + 10,
    })),
    data30d: Array.from({ length: 30 }, (_, i) => ({
      date: `2024-08-${(i + 1).toString().padStart(2, "0")}`,
      new_followers: Math.floor(Math.random() * (12 - 5 + 1)) + 5,
      new_likes: Math.floor(Math.random() * (30 - 10 + 1)) + 10,
    })),
    data90d: Array.from({ length: 90 }, (_, i) => ({
      date: `2024-06-${(i + 1).toString().padStart(2, "0")}`,
      new_followers: Math.floor(Math.random() * (12 - 5 + 1)) + 5,
      new_likes: Math.floor(Math.random() * (30 - 10 + 1)) + 10,
    })),
  };
  
  
  // ---------------------------
  // Données pour resultats-5
  // ---------------------------
  export const chartData2Resultats5 = {
    data7d: Array.from({ length: 7 }, (_, i) => ({
      date: `2024-09-0${i + 1}`,
      new_followers: Math.floor(Math.random() * (35 - 16 + 1)) + 16,
      new_likes: Math.floor(Math.random() * (20 - 10 + 1)) + 10,
    })),
    data30d: Array.from({ length: 30 }, (_, i) => ({
      date: `2024-08-${(i + 1).toString().padStart(2, "0")}`,
      new_followers: Math.floor(Math.random() * (35 - 16 + 1)) + 16,
      new_likes: Math.floor(Math.random() * (20 - 10 + 1)) + 10,
    })),
    data90d: Array.from({ length: 90 }, (_, i) => ({
      date: `2024-06-${(i + 1).toString().padStart(2, "0")}`,
      new_followers: Math.floor(Math.random() * (35 - 16 + 1)) + 16,
      new_likes: Math.floor(Math.random() * (20 - 10 + 1)) + 10,
    })),
  }
  
  // ---------------------------
  // Données pour resultats-6
  // ---------------------------
  export const chartData2Resultats6 = {
    data7d: Array.from({ length: 7 }, (_, i) => ({
      date: `2024-09-0${i + 1}`,
      new_followers: Math.floor(Math.random() * (35 - 16 + 1)) + 16,
      new_likes: Math.floor(Math.random() * (20 - 10 + 1)) + 10,
    })),
    data30d: Array.from({ length: 30 }, (_, i) => ({
      date: `2024-08-${(i + 1).toString().padStart(2, "0")}`,
      new_followers: Math.floor(Math.random() * (35 - 16 + 1)) + 16,
      new_likes: Math.floor(Math.random() * (20 - 10 + 1)) + 10,
    })),
    data90d: Array.from({ length: 90 }, (_, i) => ({
      date: `2024-06-${(i + 1).toString().padStart(2, "0")}`,
      new_followers: Math.floor(Math.random() * (35 - 16 + 1)) + 16,
      new_likes: Math.floor(Math.random() * (20 - 10 + 1)) + 10,
    })),
  }
  
  // ---------------------------
  // Regroupement pour ChartAreaInteractive2
  // ---------------------------
  export const datasets2 = {
    "resultats-1": chartData2Resultats1,
    "resultats-2": chartData2Resultats2,
    "resultats-3": chartData2Resultats3,
    "resultats-4": chartData2Resultats4,
    "resultats-5": chartData2Resultats5,
    "resultats-6": chartData2Resultats6,
  }
  